info = env.APP_NAME
echo env.APP_NAME
echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
        stage('Configuration Setup') {
            echo "Stage 1"
        }
        stage('Testing Setup') {
            echo "Stage 1"
        }
        stage('SonarQube scan') {
            echo "Stage 1"
        }
        stage('Copy Image') {
            echo "Stage 1"
        }
         stage('Stage Image') {
            echo "Stage 1"
        }