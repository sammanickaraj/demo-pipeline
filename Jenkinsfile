info = env.APP_NAME
echo env.APP_NAME
echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
        stage('Example Build') {
            steps {
                echo "Stage 1 and Step 1"
            }
        }